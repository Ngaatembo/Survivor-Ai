name: Deploy Worker

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    concurrency:
      group: survivor-production-deploy
      cancel-in-progress: false
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm ci
      - run: npm run production:guard
      - name: Apply idempotent production D1 core migration
        run: npx wrangler d1 execute survivor-ai --config worker/wrangler.toml --remote --file=./migrations/0017_production_core_tables.sql
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      - run: npm run worker:dry-run -- --var BUILD_SHA:${{ github.sha }}
      - run: npm run worker:deploy -- --var BUILD_SHA:${{ github.sha }}
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      - name: Production runtime smoke test
        run: node scripts/productionSmoke.mjs
